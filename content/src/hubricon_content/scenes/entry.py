"""The module `manim render` is pointed at. Manim loads it as a standalone file, so the
import is absolute; the package is installed editable in the content venv."""
from hubricon_content.scenes.charts import (CashCone, ChapterCard, Elasticity, Kinetic, Newsvendor, Paths,  # noqa: F401
                                            SampleSize, Screenshot, Waterfall)
